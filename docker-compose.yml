services:
  muscle-tracker:
    build: .
    container_name: muscle-tracker
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - muscle-tracker-data:/data

volumes:
  muscle-tracker-data:
